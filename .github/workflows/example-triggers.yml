# GitHub Actions 觸發條件示例
# 此文件僅用於說明，不會實際執行

# 示例1: 僅在tag推送時觸發（推薦用於發布）
# on:
#   push:
#     tags:
#       - 'v*'  # 只響應tag推送，不會在普通git push時觸發

# 示例2: 在特定分支推送時觸發
# on:
#   push:
#     branches:
#       - main
#       - develop

# 示例3: 同時響應分支和tag推送（不推薦用於發布工作流程）
# on:
#   push:
#     branches:
#       - main
#     tags:
#       - 'v*'

# 示例4: 手動觸發
# on:
#   workflow_dispatch:

# 示例5: 定期觸發
# on:
#   schedule:
#     - cron: '0 0 * * *'  # 每天午夜執行

# 示例6: 多種觸發條件組合
# on:
#   push:
#     tags:
#       - 'v*'
#   workflow_dispatch:
#     inputs:
#       environment:
#         description: 'Environment to deploy to'
#         required: true
#         default: 'staging'
